---
export const prerender = true;

import SignupNavbar from "../components/nav/signup-navbar.astro";
import { SignupPage } from "../components/pages/signup-page";
import { signupSeo } from "../data/landing-meta";
import { areSignupsClosed } from "../data/signup-deadline";
import Layout from "../layouts/layout.astro";

const seo = signupSeo();
---

<Layout
  sectionIndex={0}
  allowScroll
  title={seo.title}
  description={seo.description}
  ogImageAlt={seo.ogImageAlt}
  canonicalPathOverride={areSignupsClosed() ? "/apuntate" : "/signup"}
>
  <main id="main" class="relative min-h-dvh w-full overflow-hidden">
    <div class="relative z-10 mx-auto max-w-6xl px-0 pt-0 sm:px-4 sm:pt-10">
      <SignupNavbar />
    </div>
    <SignupPage client:load />
  </main>
</Layout>
